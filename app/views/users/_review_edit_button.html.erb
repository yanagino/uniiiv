<% if juniors? %>
  <% if current_user.approved?(user) %>
    <% if current_user.reviewing?(user) %>
      <%= link_to("編集する", "/reviews/#{user.uid}/edit", class: "btn-review-edit") %>
      <%= link_to("削除する", "/reviews/#{reviwe.id}", method: "DELETE", data: { confirm: "本当によろしいですか？" }, class: "btn-review-delete") %>
    <% end %>
  <% end %>
<% end %>